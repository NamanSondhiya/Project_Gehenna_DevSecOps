kind: PersistentVolume
apiVersion: v1
metadata:
  name: mongo-pv
  namespace: {{ .Values.namespace }}
spec:
    capacity:
        storage: 4Gi
    accessModes:
      - ReadWriteOnce
    persistentVolumeReclaimPolicy: Retain
    hostPath:
      path: /data