apiVersion: apps/v1
kind: Deployment
metadata:
  name: gehenna-frontend
  namespace: {{ .Values.namespace }}
spec:
  selector:
    matchLabels:
      app: gehenna-frontend
  template:
    metadata:
      labels:
        app: gehenna-frontend
    spec:
      initContainers:
      - name: wait-for-backend
        image: busybox
        command: ["sh", "-c", "until nc -z gehenna-backend 8002; do echo waiting for backend; sleep 3; done"]
      containers:
        - name: gehenna-frontend
          image: {{ .Values.frontendImage }}
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: gehenna-config
                  key: FRONTEND_PORT
            - name: BACKEND_URL
              valueFrom:
                configMapKeyRef:
                  name: gehenna-config
                  key: BACKEND_URL
          ports:
            - containerPort: 8001
---
apiVersion: v1
kind: Service
metadata:
  name: gehenna-frontend
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: gehenna-frontend
  ports:
    - port: 8001
      targetPort: 8001
      nodePort: {{ .Values.FNodePort }}
  type: {{ .Values.frontendsvc }}
